# 更新记录

## 1.1.2（2026-08-03）

- 将低级鼠标 Hook 与输入服务迁移到带独立消息循环的专用输入线程，托盘菜单和 WinForms UI 线程不再直接承载 Hook 回调。
- 托盘“退出”只发出停止请求，并通过 WinForms 正常的 `ExitThread` 流程结束消息循环；菜单回调中不再执行 `SendInput`、卸载 Hook 或直接终止 CLR。
- 退出时先永久阻止新的合成按下，再在输入线程上有序卸载 Hook；只对 StillTouch 已确认成功注入且尚未抬起的鼠标键补发抬键。
- 增加不依赖 CLR 线程池的独立退出看门狗；有序紧急抬键只等待 150 毫秒，若被已经卡住的 `SendInput` 挡住或立即失败，再从隔离线程按精确按键快照短时抬键；已在执行的按下若之后才返回，还会追加匹配的抬键，任何释放路径都不能阻止最终的原生进程终止。
- 自动化验证覆盖真实展开的 WinForms 菜单退出、输入消息窗彻底销毁、看门狗到原生终止的完整链路、紧急抬键永久阻塞、部分 `SendInput`、拖动释放和同位置连续点击；目标 MateBook E 仍应进行最终实机触摸验收。

## 1.1.1（2026-07-18）

- 将 `v1.1.1-rc.4` 的退出修复尝试整理为首个正式维护版。
- 该版本沿用了后台计时器与旧的强制退出链路，未能在所有托盘菜单场景中可靠避免卡死；退出实现已由 1.1.2 完整替换。

## 1.1.1-rc.4（2026-07-18）

- 首次尝试处理功能开启时从托盘右键菜单退出造成的输入卡死。
- 将部分按键释放与进程结束移到后台计时器，并加入 3 秒硬退出尝试。
- 后续实机验证确认该方案仍会复现菜单无法关闭和任务栏空白窗口栏，已由 1.1.2 的独立输入线程与独立看门狗替换。

## 1.1.1-rc.3（2026-07-18）

- 拖动抬起时不再依赖原生触摸派生的鼠标抬起，而是在最终触点坐标显式注入左键释放，并保留异常序列的兜底释放。
- 新触摸派生的左键按下会立即结束上一点击的去重窗口，避免原始 HID 报告稍晚到达时把同坐标的第二次轻点误判为重复点击。
- 增加拖动释放、旧去重状态与同坐标连续点击的回归测试。

## 1.1.1-rc.2（2026-07-18）

- 基于 `v1.1.1-rc.1`，不改变触摸识别、点击、长按、坐标或拖动逻辑。
- 托盘左键开关改为等待 `NotifyIcon.MouseClick` 与输入 Hook 调用栈完全返回后再执行，避免在自己的注入点击回调里同步卸载 Hook。
- 退出回调中只进行无锁的“停止拦截”状态切换；计时器、按键释放、Hook 拆除和资源释放统一延迟 75 毫秒执行。
- 托盘双击时间内的重复开关请求会被合并，避免一次触摸或双击连续卸载、重装输入服务。
- 保留 5 秒退出看门狗；若原生清理异常阻塞，进程仍会被强制终止并由 Windows 移除 Hook。

## 1.1.0（2026-07-17）

- 改为根据原始触摸接触的按下、移动、抬起序列主动触发点击，不再依赖目标软件先产生触摸兼容鼠标消息。
- 静止长按约 450 毫秒后主动注入右键，因此不支持原生触摸长按右键的软件和多数游戏也能收到右键输入。
- 每次触摸建立独立序列，修复同位置快速双击被合并或去重的问题。
- 原始触摸路径与低级鼠标 Hook 互相认领同一序列，避免原始点击和转换点击重复发送。
- 移除容易误判的目标进程权限预检查，直接尝试 `SendInput` 并记录真实失败结果。
- 根据触摸设备对应的显示器矩形换算触点坐标，支持副屏与负坐标布局。
- 运行期注入失败写入 EXE 同目录的 `StillTouch.log`。
- 退出时先卸载输入 Hook，再延迟释放应用上下文，降低托盘菜单回调中退出导致输入卡住的风险。
- 增加原始轻点、主动长按、拖动、多指、连续点击、去重和异常重置测试。

## 1.0.0（2026-07-16）

- 首个 StillTouch 单文件公开版本。
