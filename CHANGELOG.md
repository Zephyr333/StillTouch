# 更新记录

## 1.2.0-rc.1（2026-07-17）

- 参考 Tablet Pro / TouchMousePointer 的公开实现说明，改为覆盖虚拟桌面的透明触摸捕获层；移除低级鼠标 Hook 和 Raw Input 观察/去重链路。
- 捕获层对鼠标返回 `HTTRANSPARENT`，真实鼠标和 `SendInput` 注入鼠标可穿透到底层目标；`WM_TOUCH` 由捕获层直接消费，避免任务栏、开始菜单和应用同时收到原生触摸。
- 每次触摸接触都由 `WM_TOUCH` 的接触 ID 独立建序列，修复同一坐标连续轻点被合并的问题。
- 单指移动改为完整合成的鼠标拖动；正常抬起、第二根手指加入、读取异常、注入失败、停用和退出均有显式左键释放路径。
- 托盘左键开关改为离开通知区回调后异步执行，停用和退出先隐藏捕获层，消除触摸托盘图标时拆除 Hook 导致的重入卡死路径。
- 删除已废弃的低级鼠标 Hook、Raw HID 解析、触摸派生鼠标分类及相关依赖。
- 增加同坐标连续点击、完整拖动、抬起补全、多指中断和异常重置的回归测试。

## 1.1.0（2026-07-17）

- 改为根据原始触摸接触的按下、移动、抬起序列主动触发点击，不再依赖目标软件先产生触摸兼容鼠标消息。
- 静止长按约 450 毫秒后主动注入右键，因此不支持原生触摸长按右键的软件和多数游戏也能收到右键输入。
- 每次触摸建立独立序列，修复同位置快速双击被合并或去重的问题。
- 原始触摸路径与低级鼠标 Hook 互相认领同一序列，避免原始点击和转换点击重复发送。
- 移除容易误判的目标进程权限预检查，直接尝试 `SendInput` 并记录真实失败结果。
- 根据触摸设备对应的显示器矩形换算触点坐标，支持副屏与负坐标布局。
- 运行期注入失败写入 EXE 同目录的 `StillTouch.log`。
- 退出时先卸载输入 Hook，再延迟释放应用上下文，降低托盘菜单回调中退出导致输入卡住的风险。
- 增加原始轻点、主动长按、拖动、多指、连续点击、去重和异常重置测试。

## 1.0.0（2026-07-16）

- 首个 StillTouch 单文件公开版本。
